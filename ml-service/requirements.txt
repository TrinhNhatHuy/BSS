# Pinned to match the versions model_v2_text_channel.pkl was trained/pickled with.
# The pickle references numpy 2.x internals (numpy._core) and sklearn 1.6.x, so
# the global env (numpy 1.24) cannot load it — always use this dedicated venv.
numpy>=2.0,<3
scikit-learn==1.6.1
scipy
pandas
joblib
flask>=3.0
waitress>=3.0
psycopg2-binary>=2.9